FetchUsers
SELECT
  USER_ID,
  USER_NAME
FROM
  USERS
WHERE
  USER_TYPE = :type
  AND
  STATUS = 'ACTIVE'

SELECT
  *
FROM
  EMPLOYEES E
WHERE
  E.DEPT_ID = :deptId

Orders
SELECT
  ORDER_ID
FROM
  ORDERS
:_WHERE_AND
(
  ORDER_DATE >= :fromDate,
  ORDER_DATE <= :toDate
)

-- コメント行はそのまま出力される
